[package]
name = "refineforge-derive"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "refineforge: #[derive(LeanModel)] proc-macro — generates Lean structure declarations from Rust structs"

[lib]
proc-macro = true

[dependencies]
syn        = { version = "2", features = ["full"] }
quote      = "1"
proc-macro2 = "1"
